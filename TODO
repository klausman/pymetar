- localize the msgs returned by extractSkyConditions() and
  extractCloudInformation()
  Related: pull the messages out of the library file into an extra
  module or text/libfile
- Make pymetar Python 3.x compatible (ideally while remaining 2.6+ compatible)
